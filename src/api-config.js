/**
 * QARAH — API Configuration
 * Set QARAH_API_BASE to your deployed Cloudflare Worker URL after running:
 *   wrangler deploy
 *
 * Example: 'https://qarah-api.YOUR_SUBDOMAIN.workers.dev'
 * Leave empty to disable backend tracking (runs silently without errors).
 */
export const QARAH_API_BASE = '';
